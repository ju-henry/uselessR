# UselessR

## Overview

The package focuses on useless things, which are the ones that best highlight the useful ones.

## Installation

``` r
# Simply do:
remotes::install_github(repo = "ju-henry/uselessR")
```

## Content

You can find the following things:  

+ A raging boolean class: it is a boolean class that can lie to you based on a *rage* attribute (the higher the *rage*, the more often it lies).
